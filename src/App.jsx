import './App.css'

/*
 * ✨ ASTRO DESIGN SYSTEM PLAYGROUND ✨
 *
 * This is your sandbox! Import any component and try it out.
 *
 * Available imports:
 *
 *   import { Button } from '@shipt/design-system-buttons'
 *   import { Accordion } from '@shipt/design-system-accordion'
 *   import { Avatar } from '@shipt/design-system-avatar'
 *   import { Badge } from '@shipt/design-system-badge'
 *   import { Banner } from '@shipt/design-system-banner'
 *   import { Carousel } from '@shipt/design-system-carousel'
 *   import { Checkbox } from '@shipt/design-system-checkbox'
 *   import { Chip } from '@shipt/design-system-chip'
 *   import { Dialog } from '@shipt/design-system-dialog'
 *   import { Drawer } from '@shipt/design-system-drawer'
 *   import { Input } from '@shipt/design-system-inputs'
 *   import { Spinner } from '@shipt/design-system-loading'
 *   import { Menu } from '@shipt/design-system-menu'
 *   import { Pagination } from '@shipt/design-system-pagination'
 *   import { Popover } from '@shipt/design-system-popovers'
 *   import { RadioGroup } from '@shipt/design-system-radio-group'
 *   import { Select } from '@shipt/design-system-select'
 *   import { Switch } from '@shipt/design-system-switch'
 *   import { Table } from '@shipt/design-system-tables'
 *   import { Tabs } from '@shipt/design-system-tabs'
 *   import { Tile } from '@shipt/design-system-tile'
 *   import { Tooltip } from '@shipt/design-system-tooltip'
 *   import { Typography } from '@shipt/design-system-typography'
 *
 *   Icons:   import { SearchIcon } from '@shipt/design-system-icons'
 *   Layouts: import { Stack, Grid } from '@shipt/design-system-layouts'
 *
 * 🚀 Edit this file and save — the browser updates instantly!
 */

function App() {
  return (
    <div style={{ padding: '2rem', maxWidth: '800px', margin: '0 auto' }}>
      <h1>🚀 Astro Design System Playground</h1>
      <p>
        Edit <code>src/App.jsx</code> to start building with the design system.
        Check the comments at the top of the file for available components!
      </p>

      <hr style={{ margin: '2rem 0' }} />

      {/* 👇 Start building below! */}

    </div>
  )
}

export default App
