# Git Contribution Rules

- **Never push directly** to `main` or any protected branch.
- **Always create a feature branch** and open a pull request for any changes.
- **Never force push** (`git push --force` or `git push -f`).
